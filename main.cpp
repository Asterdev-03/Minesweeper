// Minesweeper Game

#inlcude <stdio.h>

const int MAX_SIZE = 24;

// Continues the game 
void playMinesweeper() {
    
}

// To choose the difficulty of the game
void chooseDificulty(*size) {
    int choice;
}

// To assign the user's move
void assignMoves() {}

// To count the adjacent mines of the tile
int countAdjacentMines() {
    int count;
    return count;
}

// To check validity of user's move
void isValid() {}

// To check if the tile is a mine
void isMine() {}

// To display the Board to the user
void displayBoard() {}

// To randomly place the mines
void placeMines() {}

// To open the adjacent tiles
void openAdjacentTiles() {}

int main() {
    char realBoard[25][25], myBoard[25][25];
    int boardSize;
    chooseDifficulty(&boardSize);
    placeMines();
    playMinesweeper();
    return 0;
}
